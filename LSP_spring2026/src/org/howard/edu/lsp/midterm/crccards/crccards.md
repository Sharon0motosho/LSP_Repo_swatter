TaskManager collaborates with Task because it is responsible for storing, searching, and organizing Task objects based on their properties such as taskId and status. In order to perform these responsibilities, TaskManager must interact with Task instances. However, Task does not collaborate with TaskManager because its responsibilities are limited to maintaining its own data and updating its status, so it does not need to know about or interact with the manager.
